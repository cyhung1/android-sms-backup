<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"    
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DeleteMessageActivity" >

    <TextView
        android:id="@+id/activityDeleteTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="@string/chooseBackupForDelete"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="@color/activity_restore_background" />

    <ListView
        android:id="@+id/activityDeleteListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/activityDeleteTitle"
        android:layout_centerHorizontal="true" >

    </ListView>

    <TextView
        android:id="@+id/activityDeleteMessageEmptyTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/activityDeleteListView"
        android:layout_below="@+id/activityDeleteTitle"
        android:layout_centerHorizontal="true"
        android:textColor="@color/activity_restore_background"
        android:gravity="center_vertical"
        android:text="@string/emptyResults" />

</RelativeLayout>
